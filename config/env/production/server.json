{
    "host": "${process.env.HOST}",
    "port": "${process.env.PORT} || 1337",
    "production": true,
    "proxy": {
        "enabled": true
    },
    "cron": {
        "enabled": true
    },
    "admin": {
        "autoOpen": false
    }
}